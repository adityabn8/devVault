// DevVault Extension — MV3 service worker
// Nothing to do at runtime for now; this file is required by the manifest.
chrome.runtime.onInstalled.addListener(() => {
  console.log('DevVault extension installed.');
});
